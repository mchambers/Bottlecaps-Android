<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent">

    <TextView
        android:text="BottleCaps"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:textSize="48sp"
        android:textStyle="bold"
            android:layout_marginTop="40dp"
            android:id="@+id/splashTitle"/>

     <Button android:id="@+id/splashPlayButton"
             android:onClick="onPlayButtonClick"
             android:text="Play BottleCaps"
             android:layout_width="fill_parent"
             android:layout_height="60dp"
             android:gravity="center"
             android:textSize="20sp"
             android:layout_marginTop="50dp"
             android:layout_marginLeft="10dp"
             android:layout_marginRight="10dp"
             android:layout_below="@id/splashTitle"/>

    <Button android:id="@+id/splashPlayKidsMode"
            android:onClick="onKidsModeClick"
            android:text="Play in Kids Mode"
            android:layout_below="@id/splashPlayButton"
            android:layout_marginTop="10dp"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_height="60dp"
            android:textSize="20dp"
            android:layout_width="fill_parent"
            />

    <Button android:id="@+id/splashMenuButton"
            android:text="Menu"
            android:layout_width="wrap_content"
            android:gravity="center"
            android:textSize="15sp"
            android:layout_below="@id/splashPlayKidsMode"
            android:layout_marginTop="55dp"
            android:layout_height="45dp"
            android:layout_centerHorizontal="true"/>

</RelativeLayout>